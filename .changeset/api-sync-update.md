---
"@blindpay/node": minor
---

API sync - Updated SDK to match latest API specification

This change ensures the SDK is fully aligned with the current API state including all enum types, receiver fields, and virtual account configurations. All changes from the API reference have been validated and are now properly implemented.