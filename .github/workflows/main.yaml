name: Main

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  lint:
    uses: ./.github/workflows/lint.yaml
  tests:
    needs: lint
    uses: ./.github/workflows/tests.yaml
  publish:
    uses: ./.github/workflows/publish.yaml